name: Completed Branch Cleanup

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  completed:
    name: Cleanup completed
    runs-on: ubuntu-latest
    steps:
      - name: Report status
        run: echo 'One-time branch cleanup completed. No automatic branch deletion is enabled.'
